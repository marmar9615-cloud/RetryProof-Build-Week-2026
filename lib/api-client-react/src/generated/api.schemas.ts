/**
 * Generated by orval v8.5.3 🍺
 * Do not edit manually.
 * Api
 * NeverGuess API
 * OpenAPI spec version: 0.1.0
 */
export interface HealthStatus {
  status: string;
}

export interface AuthUser {
  id: string;
  /** @nullable */
  email: string | null;
  /** @nullable */
  firstName: string | null;
  /** @nullable */
  lastName: string | null;
  /** @nullable */
  profileImageUrl: string | null;
}

export interface AuthUserEnvelope {
  user: AuthUser | null;
}

export interface MobileTokenExchangeRequest {
  /** @minLength 1 */
  code: string;
  /** @minLength 1 */
  code_verifier: string;
  /** @minLength 1 */
  redirect_uri: string;
  /** @minLength 1 */
  state: string;
  /** @minLength 1 */
  nonce?: string;
}

export interface MobileTokenExchangeSuccess {
  token: string;
}

export const LogoutSuccessValue = {
  success: true,
} as const;
export type LogoutSuccess = typeof LogoutSuccessValue;

export interface ErrorEnvelope {
  error: string;
  code?: string;
}

export interface TrialStatus {
  authenticated: boolean;
  trialEligible: boolean;
  /** @minimum 0 */
  used: number;
  /** @minimum 1 */
  limit: number;
  /** @minimum 0 */
  remaining: number;
}

export type AuditStatus = (typeof AuditStatus)[keyof typeof AuditStatus];

export const AuditStatus = {
  pending: "pending",
  running: "running",
  ingested: "ingested",
  analyzing: "analyzing",
  done: "done",
  error: "error",
} as const;

export type RiskSeverity = (typeof RiskSeverity)[keyof typeof RiskSeverity];

export const RiskSeverity = {
  critical: "critical",
  high: "high",
  medium: "medium",
  low: "low",
} as const;

/**
 * A concrete pointer into the ingested repo backing a risky assumption.
`verified` is stamped server-side after cross-checking the cited path
against the ingested file tree.

 */
export interface RiskEvidence {
  path: string;
  /** @nullable */
  quote?: string | null;
  verified?: boolean;
}

export interface RiskyAssumption {
  title: string;
  severity: RiskSeverity;
  detail: string;
  /** Absent on reports generated before citations shipped. */
  evidence?: RiskEvidence[];
}

export type AffectedAreaAction =
  (typeof AffectedAreaAction)[keyof typeof AffectedAreaAction];

export const AffectedAreaAction = {
  modify: "modify",
  create: "create",
  read: "read",
} as const;

/**
 * Blast radius entry — a file or directory the requested change is
expected to touch. Paths are model-proposed; `create` entries may
not exist in the tree yet.

 */
export interface AffectedArea {
  path: string;
  reason: string;
  action: AffectedAreaAction;
}

export interface AcceptanceCriterion {
  title: string;
  detail: string;
}

export interface PromptPack {
  replit: string;
  cursor: string;
  copilot: string;
  claudeCode: string;
  codex: string;
}

export type Verdict = (typeof Verdict)[keyof typeof Verdict];

export const Verdict = {
  safe: "safe",
  caution: "caution",
  block: "block",
} as const;

export interface TestPlan {
  vitest: string;
  playwright: string;
}

export type SmokeCheckStatus =
  (typeof SmokeCheckStatus)[keyof typeof SmokeCheckStatus];

export const SmokeCheckStatus = {
  pass: "pass",
  fail: "fail",
  warn: "warn",
  skip: "skip",
} as const;

export interface SmokeCheck {
  id: string;
  label: string;
  status: SmokeCheckStatus;
  metric?: string;
  detail: string;
}

export interface SmokeTestResults {
  ranAt: string;
  url: string;
  skipped: boolean;
  skipReason?: string;
  checks: SmokeCheck[];
}

export type ModelCatalogEntryProvider =
  (typeof ModelCatalogEntryProvider)[keyof typeof ModelCatalogEntryProvider];

export const ModelCatalogEntryProvider = {
  anthropic: "anthropic",
  openai: "openai",
  google: "google",
  "x-ai": "x-ai",
} as const;

export type ModelCatalogEntryTier =
  (typeof ModelCatalogEntryTier)[keyof typeof ModelCatalogEntryTier];

export const ModelCatalogEntryTier = {
  standard: "standard",
  premium: "premium",
} as const;

/**
 * One selectable analysis model. Static catalog fields are always
present; live fields (contextLength, pricing) are enriched from the
public OpenRouter catalog and are null when enrichment is unavailable.

 */
export interface ModelCatalogEntry {
  /** OpenRouter model id, e.g. "anthropic/claude-fable-5". */
  id: string;
  displayName: string;
  provider: ModelCatalogEntryProvider;
  tier: ModelCatalogEntryTier;
  proOnly: boolean;
  isDefault: boolean;
  /** Whether the current caller may run audits on this model. */
  available: boolean;
  /**
   * Why the model is unavailable to this caller, when available is
false. One of "sign-in-required" or "pro-required".

   * @nullable
   */
  lockReason: string | null;
  /**
   * Short, capability-based description. No benchmark claims.
   * @nullable
   */
  blurb: string | null;
  /** @nullable */
  contextLength: number | null;
  /**
   * USD per million prompt tokens, from the live catalog.
   * @nullable
   */
  promptPricePerMTok: number | null;
  /**
   * USD per million completion tokens, from the live catalog.
   * @nullable
   */
  completionPricePerMTok: number | null;
}

export interface ModelsResponse {
  models: ModelCatalogEntry[];
  /** Catalog id used when an audit has no explicit model. */
  defaultModel: string;
  /** True when live OpenRouter enrichment succeeded. */
  live: boolean;
}

export type AnalysisUsageGateway =
  (typeof AnalysisUsageGateway)[keyof typeof AnalysisUsageGateway];

export const AnalysisUsageGateway = {
  "replit-ai-integrations": "replit-ai-integrations",
  "openrouter-direct": "openrouter-direct",
} as const;

/**
 * Model response receipt captured from the analysis provider.
 */
export interface AnalysisUsage {
  model: string;
  requestedModel: string;
  reasoningEffort: string;
  gateway: AnalysisUsageGateway;
  /** @nullable */
  requestId?: string | null;
  /**
   * @minimum 0
   * @nullable
   */
  promptTokens?: number | null;
  /**
   * @minimum 0
   * @nullable
   */
  completionTokens?: number | null;
  /**
   * @minimum 0
   * @nullable
   */
  totalTokens?: number | null;
  /**
   * @minimum 0
   * @nullable
   */
  reasoningTokens?: number | null;
  generatedAt: string;
}

export interface Report {
  id: string;
  auditId: string;
  source: string;
  architectureSummary: string;
  mermaidGraph: string;
  riskyAssumptions: RiskyAssumption[];
  acceptanceCriteria: AcceptanceCriterion[];
  promptPack: PromptPack;
  rolloutNotes: string;
  smokeTestResults?: SmokeTestResults | null;
  /** @nullable */
  smokeScreenshotUrl?: string | null;
  analysisUsage?: AnalysisUsage | null;
  /**
   * @minimum 0
   * @maximum 100
   */
  riskScore: number;
  verdict: Verdict;
  testPlan?: TestPlan | null;
  affectedAreas?: AffectedArea[] | null;
  /** @nullable */
  shareSlug?: string | null;
  createdAt: string;
}

/**
 * Verdict receipt from a superseded report, snapshotted when a re-run
replaced it. Newest first.

 */
export interface RunHistoryEntry {
  id: string;
  /** @nullable */
  model: string | null;
  verdict: Verdict | null;
  /** @nullable */
  riskScore: number | null;
  createdAt: string;
}

export interface ShareLinkResponse {
  shareSlug: string;
  shareUrl: string;
}

export const RevokeShareLinkResponseValue = {
  success: true,
} as const;
export type RevokeShareLinkResponse = typeof RevokeShareLinkResponseValue;

export const DeleteAuditResponseValue = {
  success: true,
} as const;
export type DeleteAuditResponse = typeof DeleteAuditResponseValue;

export interface PublicAudit {
  requestedChange: string;
  /** @nullable */
  githubUrl: string | null;
  /** @nullable */
  liveUrl: string | null;
  /** @nullable */
  detectedFramework: string | null;
  /** @nullable */
  detectedPackageManager: string | null;
  /** @nullable */
  detectedDbLayer: string | null;
  /** @nullable */
  detectedAuthLayer: string | null;
  createdAt: string;
}

export interface PublicReport {
  report: Report;
  audit: PublicAudit;
}

export type IngestionSource =
  (typeof IngestionSource)[keyof typeof IngestionSource];

export const IngestionSource = {
  github: "github",
  demo: "demo",
} as const;

export type AuditEventPhase =
  (typeof AuditEventPhase)[keyof typeof AuditEventPhase];

export const AuditEventPhase = {
  ingesting: "ingesting",
  "detecting-stack": "detecting-stack",
  "calling-llm": "calling-llm",
  "running-smoke": "running-smoke",
  done: "done",
  error: "error",
} as const;

/**
 * One Server-Sent Event in the audit live stream.
 */
export interface AuditEvent {
  /** Unix epoch milliseconds when the event was published. */
  ts: number;
  phase: AuditEventPhase;
  message: string;
}

export interface Audit {
  id: string;
  userId: string;
  /** @nullable */
  githubUrl: string | null;
  /** @nullable */
  liveUrl: string | null;
  requestedChange: string;
  /**
   * OpenRouter model id this audit's analysis runs on. Null means the
server default (audits created before model selection shipped, or
users who never touched the selector).

   * @nullable
   */
  model: string | null;
  status: AuditStatus;
  ingestionSource: IngestionSource | null;
  /** @nullable */
  detectedFramework: string | null;
  /** @nullable */
  detectedPackageManager: string | null;
  /** @nullable */
  detectedDbLayer: string | null;
  /** @nullable */
  detectedAuthLayer: string | null;
  /** @nullable */
  routesFolder: string | null;
  deploymentClues: string[] | null;
  fileTreeSample: string[] | null;
  /** @nullable */
  fileCount: number | null;
  /** @nullable */
  ingestionError: string | null;
  /**
   * @minimum 0
   * @maximum 100
   * @nullable
   */
  riskScore?: number | null;
  verdict?: Verdict | null;
  /** True when this audit belongs to the read-only demo gallery rather
than the caller. Only meaningful in list responses for anonymous
callers; absent elsewhere.
 */
  isDemo?: boolean;
  createdAt: string;
  updatedAt: string;
}

/**
 * Opaque session token — `Bearer <sid>`.
 */
export type AuthorizationSessionHeaderParameter = string;

export type BeginBrowserLoginParams = {
  /**
   * Relative path to redirect to after login (must start with `/`).
   */
  returnTo?: string;
};

export type HandleBrowserLoginCallbackParams = {
  code?: string;
  state?: string;
  iss?: string;
};

export type CreateAuditBody = {
  /** @nullable */
  githubUrl?: string | null;
  /** @nullable */
  liveUrl?: string | null;
  /** @minLength 1 */
  requestedChange: string;
  /**
   * Optional, per-request GitHub personal access token. Held in
server memory only for this single ingestion job. Never
persisted to the database, never logged, never returned in
any response. Use this when the public rate limit is
exhausted or you want extra headroom for ingesting a
larger public repository.

   * @nullable
   */
  githubToken?: string | null;
  /**
   * Optional analysis model id from GET /models. Must be one
of the catalog ids. Anonymous trial audits may only use
the default model. Premium (proOnly) models require a Pro
plan. Omitted or null means the server default.

   * @nullable
   */
  model?: string | null;
};

export type RerunAuditBody = {
  /**
   * Optionally re-run the audit on a different catalog model.
Same validation and plan gating as audit creation. Omitted
or null re-runs with the audit's existing model.

   * @nullable
   */
  model?: string | null;
};
