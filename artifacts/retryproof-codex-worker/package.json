{
  "name": "@workspace/retryproof-codex-worker",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "rm -rf dist && tsc -p tsconfig.json",
    "start": "node --enable-source-maps dist/index.js",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run src"
  },
  "dependencies": {
    "@openai/codex-sdk": "0.144.3"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "typescript": "~5.9.2",
    "vitest": "^4.1.5"
  }
}
